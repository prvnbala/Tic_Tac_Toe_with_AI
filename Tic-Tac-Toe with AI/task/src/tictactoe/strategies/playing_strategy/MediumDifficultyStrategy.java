package tictactoe.strategies.playing_strategy;

import tictactoe.models.Board;

public class MediumDifficultyStrategy implements PlayingStrategy {

    @Override
    public int[] findNextMove(Board board) {
        return new int[0];
    }
}
